build:
  docker:
    web: Dockerfile
run:
  web: PYTHONPATH=`pwd`/.. gunicorn --bind 0.0.0.0:8000 app.wsgi:application
release:
  image: web
  command:
    - python backend/manage.py collectstatic --noinput